@model List<CityWeather>;

<div class="page-content ">
    <div class="weather">

        <h1> Weather</h1>
    </div>
    <div class="main  ">
        @foreach (CityWeather city in Model)
        {
            <partial name="_CiryPartial" model="city" />
        }
    </div>
</div>
